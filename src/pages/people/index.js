import React from 'react';
import { Layout, PeopleList } from '../../components';

const PeoplePage = () => (
    <Layout>
        <div className="container">
            <PeopleList />
        </div>
    </Layout>
);

export default PeoplePage;
